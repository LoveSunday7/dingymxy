# ============================================
# 黑色小猫咪的GitHub小窝 - 部署配置
# ============================================

# 前端服务
frontend:
  host: "0.0.0.0"
  port: 5500

# 后端服务
backend:
  host: "0.0.0.0"
  port: 8000
  workers: 1
  # 生产环境关闭debug
  debug: false

# 域名（生产环境）
domain: ""
